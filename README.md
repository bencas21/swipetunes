# swipetunes

## Getting Started

The dev container automatically installs all dependencies. Once the container is ready, you can start working immediately:

**Backend API:**
```bash
cd backend
python3 src/health.py
```

**Frontend:**
```bash
cd web
npm run dev
```

**Infrastructure (CDK):**
```bash
cd infra
cdk synth
```