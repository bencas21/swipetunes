{
  "name": "swipetunes",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "root",
  "postCreateCommand": "pip3 install -r infra/requirements.txt && pip3 install -r infra/requirements-dev.txt && pip3 install -r backend/requirements.txt && cd web && npm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  }
}
